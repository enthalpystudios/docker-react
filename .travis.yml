language: generic
sudo: required
services: 
    - docker

before_install: 
    - docker build -f Dockerfile.dev -t enthalpystudios/docker-react .

script:
    - docker run -e CI=true enthalpystudios/docker-react npm run test